# Environment Configuration for GitHub Codespaces
# Copy this file to .env and fill in your credentials before running docker-compose

# ==============================================================================
# REQUIRED: Google OAuth Credentials
# ==============================================================================
# Get your Google Client ID from: https://console.cloud.google.com/
# 1. Create a new OAuth 2.0 Client ID (or use existing)
# 2. Add your Codespaces URL to authorized origins and redirect URIs
#    Example: https://supreme-system-xjrwrwgxvxcj9w-5173.app.github.dev
# 3. Copy the Client ID here
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# ==============================================================================
# REQUIRED: JWT Secret (for authentication tokens)
# ==============================================================================
# Generate a secure random string (minimum 32 characters)
# Run this command to generate one:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-secure-random-64-character-string-generated-above

# ==============================================================================
# REQUIRED: Codespaces URLs (Dynamic - Update These!)
# ==============================================================================
# GitHub Codespaces generates unique URLs for each port
# Format: https://<codespace-name>-<port>.app.github.dev
#
# To find your URLs:
# 1. Start your Codespace
# 2. In the PORTS tab (bottom panel), you'll see forwarded ports
# 3. Copy the "Forwarded Address" for ports 5173 and 3001
# 4. Update the URLs below

# Frontend URL (port 5173) - Update with YOUR Codespace URL
VITE_API_URL=https://supreme-system-xjrwrwgxvxcj9w-3001.app.github.dev

# Backend CORS - Allow requests from frontend (port 5173) - Update with YOUR Codespace URL
FRONTEND_URL=https://supreme-system-xjrwrwgxvxcj9w-5173.app.github.dev

# ==============================================================================
# Database Configuration (Pre-configured for Docker Compose)
# ==============================================================================
# These are already configured in docker-compose.yml - no need to change
# DATABASE_URL=postgresql://shader_user:shader_password@postgres:5432/shader_playground
# PORT=3001
# NODE_ENV=production

# ==============================================================================
# Quick Setup Instructions for Codespaces
# ==============================================================================
# 1. Copy this file:
#    cp .env.codespaces.example .env
#
# 2. Get your Google Client ID from Google Cloud Console
#
# 3. Generate JWT secret:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 4. Start Docker to see your Codespace URLs:
#    docker-compose up -d postgres
#    (Check PORTS tab for the URLs)
#
# 5. Update VITE_API_URL and FRONTEND_URL above with your Codespace URLs
#
# 6. Rebuild and start all services:
#    docker-compose down
#    docker-compose up --build
#
# 7. Open the frontend URL (port 5173) from the PORTS tab
