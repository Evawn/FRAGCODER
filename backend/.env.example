# Database Configuration
# PostgreSQL is used for both development and production.
# For local development, start PostgreSQL with: docker compose up postgres -d
DATABASE_URL="postgresql://shader_user:shader_password@localhost:5432/shader_playground"

# JWT Secret (REQUIRED in production)
# Use a strong random string (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secret-key-here-min-32-chars"

# Google OAuth Client ID (REQUIRED)
# Get this from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-oauth-client-id"

# Server Configuration
PORT=3001

# Frontend URL (for CORS configuration)
# In production, set this to your deployed frontend URL
FRONTEND_URL="http://localhost:5173"
